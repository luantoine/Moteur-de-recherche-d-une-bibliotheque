import React from 'react';
import HeadBar from "./HeadBar";

const Loading = () => {
    return (
        <div >
            <HeadBar/>
            <h1>Chargement des livres...</h1>
        </div>
    );
};

export default Loading;